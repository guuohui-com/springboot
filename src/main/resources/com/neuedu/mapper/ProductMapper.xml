<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.neuedu.dao.ProductMapper" >
  <resultMap id="BaseResultMap" type="com.neuedu.pojo.Product" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <id column="id" property="id" jdbcType="INTEGER" />
    <result column="category_id" property="categoryId" jdbcType="INTEGER" />
    <result column="name" property="name" jdbcType="VARCHAR" />
    <result column="subtitle" property="subtitle" jdbcType="VARCHAR" />
    <result column="main_image" property="mainImage" jdbcType="VARCHAR" />
    <result column="price" property="price" jdbcType="DECIMAL" />
    <result column="stock" property="stock" jdbcType="INTEGER" />
    <result column="status" property="status" jdbcType="INTEGER" />
    <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
    <result column="update_time" property="updateTime" jdbcType="TIMESTAMP" />
    <result column="sub_images" property="subImages" jdbcType="LONGVARCHAR" />
    <result column="detail" property="detail" jdbcType="LONGVARCHAR" />
  </resultMap>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    delete from neuedu_product
    where id = #{id,jdbcType=INTEGER}
  </delete>
  <insert id="insert" parameterType="com.neuedu.pojo.Product" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    insert into neuedu_product (id, category_id, name, 
      subtitle, main_image, price, 
      stock, status, create_time, 
      update_time, sub_images, detail
      )
    values (#{id,jdbcType=INTEGER}, #{categoryId,jdbcType=INTEGER}, #{name,jdbcType=VARCHAR}, 
      #{subtitle,jdbcType=VARCHAR}, #{mainImage,jdbcType=VARCHAR}, #{price,jdbcType=DECIMAL}, 
      #{stock,jdbcType=INTEGER}, #{status,jdbcType=INTEGER}, now(),
      now(), #{subImages,jdbcType=LONGVARCHAR}, #{detail,jdbcType=LONGVARCHAR}
      )
  </insert>
  <update id="updateByPrimaryKey" parameterType="com.neuedu.pojo.Product" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    update neuedu_product
    <set>
      <if test="categoryId!=null">
        category_id = #{categoryId,jdbcType=INTEGER},
      </if>
      <if test="name!=null">
        name = #{name,jdbcType=VARCHAR},
      </if>
      <if test="subtitle!=null">
        subtitle = #{subtitle,jdbcType=VARCHAR},
      </if>
      <if test="mainImage!=null">
        main_image = #{mainImage,jdbcType=VARCHAR},
      </if>
      <if test="price!=null">
        price = #{price,jdbcType=DECIMAL},
      </if>
      <if test="stock!=null">
        stock = #{stock,jdbcType=INTEGER},
      </if>
      <if test="status!=null">
        status = #{status,jdbcType=INTEGER},
      </if>
      <if test="subImages!=null">
        sub_images = #{subImages,jdbcType=LONGVARCHAR},
      </if>
      <if test="detail!=null">
        detail = #{detail,jdbcType=LONGVARCHAR},
      </if>
      create_time = now(),
      update_time =now(),
    </set>
    where id = #{id,jdbcType=INTEGER}
  </update>
  <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.Integer" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    select id, category_id, name, subtitle, main_image, price, stock, status, create_time, 
    update_time, sub_images, detail
    from neuedu_product
    where id = #{id,jdbcType=INTEGER}
  </select>
  <select id="selectAll" resultMap="BaseResultMap" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    select id, category_id, name, subtitle, main_image, price, stock, status, create_time,
    update_time, sub_images, detail
    from neuedu_product
  </select>

  <update id="upperShelf" parameterType="int">
     update neuedu_product SET status = 1 WHERE id=#{id}
  </update>

  <update id="downShelf" parameterType="int">
    update neuedu_product SET status = 0 WHERE id=#{id}
  </update>

  <select id="pageSearch" parameterType="map" resultMap="BaseResultMap" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    select id, category_id, name, subtitle, main_image, price, stock, status, create_time,
    update_time, sub_images, detail
    from neuedu_product limit #{start},2
  </select>

  <select id="springbootPageSearch" parameterType="map" resultMap="BaseResultMap" >
    select id, category_id, name, subtitle, main_image, price, stock, status, create_time,
    update_time, sub_images, detail
    from neuedu_product limit #{start},#{size}
  </select>

  <select id="searchProduct" parameterType="map" resultMap="BaseResultMap" >
    select id, category_id, name, subtitle, main_image, price, stock, status, create_time,
    update_time, sub_images, detail
    from neuedu_product
    <where>
      <if test="productId!=null">
        and id=#{productId}
      </if>

      <if test="productName!=null">
        and name like #{productName}
      </if>
      limit #{start},#{size}
    </where>

  </select>

  <select id="searchProductAll" parameterType="map" resultMap="BaseResultMap" >
    select id, category_id, name, subtitle, main_image, price, stock, status, create_time,
    update_time, sub_images, detail
    from neuedu_product
    <where>
      <if test="productId!=null">
        and id=#{productId}
      </if>

      <if test="productName!=null">
        and name like #{productName}
      </if>
    </where>

  </select>


  <select id="portalSearchproduct" parameterType="map" resultMap="BaseResultMap" >
    select id, category_id, name, subtitle, main_image, price, stock, status, create_time,
    update_time, sub_images, detail
    from neuedu_product
    <where>
      <if test="integerSet!=null and integerSet.size>0">
        AND category_id IN
        <foreach collection="integerSet" item="categroyId" open="(" close=")" separator=",">
          #{categoryId}
        </foreach>
      </if>

      <if test="productName!=null">
        and name like #{productName}
      </if>
    </where>

    <if test="orderBy!=null">
      ORDER BY #{orderBy}
    </if>
    limit #{start},#{size}
  </select>

  <select id="portalSearchproductAll" parameterType="map" resultMap="BaseResultMap" >
    select id, category_id, name, subtitle, main_image, price, stock, status, create_time,
    update_time, sub_images, detail
    from neuedu_product
    <where>
      <if test="integerSet!=null and integerSet.size>0">
        AND category_id IN
        <foreach collection="integerSet" item="categroyId" open="(" close=")" separator=",">
          #{categoryId}
        </foreach>
      </if>

      <if test="productName!=null">
        and name like #{productName}
      </if>
    </where>

    <if test="orderBy!=null">
      ORDER BY #{orderBy}
    </if>
  </select>

  <update id="updateStock" parameterType="map">
    update neuedu_product SET stock = #{stock} WHERE id=#{productId}
  </update>

  <select id="selectByIsHot" resultMap="BaseResultMap">
     select id, category_id, name, subtitle, main_image, price, stock, status, create_time,
    update_time, sub_images, detail
    from neuedu_product
    WHERE is_hot=1
  </select>
</mapper>